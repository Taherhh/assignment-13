//assignment no 13
var transporttionMode = ["car", "motorcycle", "bicycle", "bus"];
for (var i = 0; i < transporttionMode.length; i++) {
    console.log("I would to own a ".concat(transporttionMode[i]));
}
