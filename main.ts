//assignment no 13

let transporttionMode: string[] = ["car", "motorcycle", "bicycle", "bus"];

for (let i = 0; i < transporttionMode.length; i++) {
    console.log(`I would to own a ${transporttionMode[i]}` );
    
}